# Production frontend — baked at Docker build time via NEXT_PUBLIC_* args.
# Same-origin deploy (recommended): leave empty; browser calls /api/v1 on your domain.
NEXT_PUBLIC_API_URL=
INTERNAL_API_URL=http://backend:8000
NEXT_PUBLIC_LOG_CLIENT_ERRORS=true
